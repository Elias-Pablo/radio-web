---
import { cn } from "../../lib/utils";

interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...props } = Astro.props;
---

<div class={cn("rounded-xl border bg-card text-card-foreground shadow", className)} {...props}>
  <slot />
</div>
